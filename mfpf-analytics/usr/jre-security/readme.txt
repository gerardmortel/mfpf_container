Place your JRE cacerts truststore file, JRE policy jar files and other JRE security related files here if it should be updated on the Container.

The files placed here will be copied onto the JAVA_HOME/jre/lib/security/ folder on the Container.